// const presentOrderBtn = document.querySelector('.present__order-btn'),
//       pageOverlayMmodal = document.querySelector('.page__overlay_modal'),
//       modalClose = document.querySelector('.modal__close');

// const hendlerModal = (openBtn, modal, openSelector, closeTrigger) => {
//   openBtn.addEventListener('click', () => {
//     modal.classList.add(openSelector);
//   });

//   closeTrigger.addEventListener('click', () => {
//     modal.classList.remove(openSelector);
//   });
// };

// hendlerModal(presentOrderBtn, pageOverlayMmodal, 'page__overlay_modal_open', modalClose);
